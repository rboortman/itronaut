%a{:href => content_path(@home)}
	.logo
		%img{:src => '/assets/overall/itronaut.png'}

%a{:href => content_path(@home)}
	.menu.first
		.text
			home

.line

%a{:href => content_path(@home)}
	.menu
		.text
			bla

.line

%a{:href => content_path(@home)}
	.menu
		.text
			bla

.line

%a{:href => content_path(@home)}
	.menu
		.text
			bla

.line

%a{:href => content_path(@contact)}
	.menu.last
		.text
			contact